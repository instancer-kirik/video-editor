{
  "name": "zig-wasm-video-editor-test",
  "version": "0.1.0",
  "description": "Test environment for Zig WASM video editor",
  "type": "module",
  "scripts": {
    "serve": "node server.js",
    "start": "npm run serve",
    "dev": "npm run serve -- --watch",
    "build": "cd ../zig && zig build",
    "test": "npm run build && npm run serve"
  },
  "keywords": [
    "zig",
    "wasm",
    "video-editor",
    "tiktok",
    "webassembly"
  ],
  "author": "Video Editor Team",
  "license": "MIT",
  "devDependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "compression": "^1.7.4",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
